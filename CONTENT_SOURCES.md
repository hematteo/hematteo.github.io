# Content and asset sources

The SRP explorer at `/research/prism/` leads with the same-token bug comparison. `scripts/export-prism.py` reads `interactive/token_decomposition/public/data/qwen2b_k256/decompositions/bug_insect_error/5/9584.json` and `bug_software_insect/7/9584.json`, plus `examples.json`, from `/Users/m/Desktop/SRP/readout_prism_workspace`. Both records score the same leading-space token (ID 9584), using the same dictionary. These are raw single-token companion-export measurements for the paper's worked-case prompts, not the paper's two different token contrasts or its separately centered context-difference plot. Four illustrative features (36, 18302, 21804, 5680) have explicit recorded values in both contexts. They are not claimed to be the largest four terms or a complete sense partition. Missing truncated entries are never interpreted as zero. The context-difference mode subtracts software minus insect values, including full score, offset, remainder and residual. Animation is a visual transition between recorded endpoints, not a model intervention.

English/Chinese London examples use `results/jlens/cross_lens_v2_{en,zh}.json`, record fac_03, layer 26, position -1, with full-precision target scores for the reported tokens. Prompt and feature-sum percentages were checked against `arxiv_upload/source/sections/beyond_token_identity.tex`. Lens controls update a local chart of the dominant feature, other features, offset, and residual. No labels from another dictionary are attached. Conditions are discrete; no intermediate lens or hidden state is simulated.

`public/research/prism-layer-attribution.png` is an unaltered copy of `Sparse_Readout_Prism/figures/proto_token_lens/qwen2b_32x_prism_dla_verify_assume/qwen2b_32x_prism_dla_semantic_table.png`. Text was verified against `arxiv_upload/source/appendices/feature_resolved_dla.tex`. The original static figure, in an optional deeper section, preserves its red/blue convention and rounded values. Readout residual and component additivity error are explicitly separate.

All score accounts recover the recorded full score within 1e-5 when shown features, undisplayed features, offset, and reconstruction residual are added. The previous password record is retained in the downloadable JSON and exporter for reproducibility but is no longer displayed on the page. Browser checks cover both contexts and their difference, accounting, local lens updates, feature inspection, reduced motion, 320/390px layouts, static fallback, and runtime errors.

Native research chart redesign: `scripts/build-research-charts.py` reads `main_claim_k8_feature_interventions.csv` for readout interventions and the literal `hidden_projection_df` rows in the paper's plotting script for hidden-state projection. The availability chart preserves the exact arrays, display-space x coordinates, and labelled ticks from `Learning_to_Read_Out/figures/readout_coordination/make_sva_availability_expression_69b.py`; that source itself records recovered display coordinates, so no new checkpoint schedule is inferred. Convergence values use that script's `LADDER`. Timing values are transcribed from the labelled paper figure: multipliers 0.25/1/4, reorganization steps 2048/1024/512, median peak steps 128/64/32, and baseline-matching shortened warmup. Native charts use percentage units and retain per-panel baselines, exact-value tables, full task labels and original-figure links. The feature example stage buttons use each measured curve's coordinates at step 16, its verified peak, and step 143000.

The research explorer also includes three selected Pythia-1B feature examples (IDs 1227, 15921, 2228), exported by `scripts/export-feature-examples.py` from `feature_cards_representative.csv` and the cached decoder norms. Peak checkpoints and norm maxima were checked against the CSV; token ordering is preserved and display whitespace trimmed. The individual curves use all positive training checkpoints and normalization identical to the trajectory plot. The labels are qualitative descriptions, not ground-truth concepts.

The explorer's intervention and timing images are unaltered renders of `Desktop/LRO/Learning_to_Read_Out/figures/contrastive_task_localization/contrastive_task_localization_1x3.pdf` and `figures/recipe_control/timing_dose_response.pdf`. Captions and scope were checked against `appendix/readout_swap_behavioral.tex` and the recipe-control section of `main.tex`. The intervention section identifies the fixed hidden-state/readout pairing, differing baselines, held-out split, and exploratory 26-example facts result. The timing section states the 31M-parameter setting and one run per condition. The existing agreement image is reused with probe/behavior distinctions retained.

The interactive trajectory explorer lives at `/research/trajectories/`, with model selection, replay, inspection, and a reading guide. The homepage uses a single-model Pythia-1B preview built by `scripts/build-trajectory-preview.py` from all 1,200 existing sampled curves. It retains own-peak normalization and all 31 positive checkpoints on a logarithmic step axis, with no smoothing. The preview links to the full four-model explorer and does not fetch trajectory data. The supporting agreement figure remains under Method & scope.

Prepared 8 September 2026. The approved brief is a compact research index for research hiring and collaboration, with the existing drawer retained as a secondary page.

- Biographical facts and engineering achievements: Matteo's canonical `career-ops/cv.md` and `modes/_profile.md`.
- Public CV: `/cv/matteo-he-research.pdf`, rebuilt from `cv-source/matteo-he-research.tex`, adapted from the canonical AI-safety CV source. Broadened the research profile, added GRE 340/340, identified the dissertation projects, and replaced direct email/phone with a website contact link. Canonical application CVs are unchanged.
- Positioning and result selection were cross-checked against `career-ops/output/cvs/canonical/ai-safety-alignment-interpretability.pdf`, `career-ops/output/cvs/drafts/matteo-he-neel-nanda-mats-winter-2027.pdf`, and the research-dense MATS draft `career-ops/output/cvs/drafts/mhcv.pdf`. The MATS-specific drafts are inputs to the website copy, not public downloads.
- Latest user direction: the dissertation should not define the entire research trajectory. Broader AI-safety interests come from `modes/_profile.md` and are labeled as interests, not completed safety research. GRE 340/340 is explicitly confirmed by the user and supported by the Nanda draft (170 quantitative, 170 verbal).
- Sparse Readout Prism: https://arxiv.org/abs/2609.01936 and https://github.com/hematteo/sparse-readout-prism (README and CITATION.cff). First author; preprint and under review, not an accepted conference paper. Homepage results use the CV's scoped findings: the Qwen3.5-9B fitting-language confound and 8.9–17.3 percentage-point coverage improvement over the strongest of six geometric baselines across six readouts without softcaps.
- Learning to Read Out: https://github.com/hematteo/learning-to-read-out (README and CITATION.cff). First author; manuscript under review. Public code and guided notebooks; no fabricated arXiv URL or acceptance. Homepage results preserve the CV's scope: Pythia-6.9B timing result, a fixed Pythia-1B step-1000 feature ablation, and single-seed 31M-parameter learning-rate controls.
- Attention-head interpretability: canonical CV's 2024 St Andrews honours dissertation. No public dissertation URL was established, so no paper/code link is invented. Evaluation figures appear with their experimental scope.
- `vigil-gpu`: https://github.com/hematteo/vigil and https://pypi.org/project/vigil-gpu/.
- The recorded demo link points to the existing `docs/demo.gif` in the public vigil repository, verified through the GitHub contents API. No simulated project screenshot was created.
- The future research questions and expandable research note were removed at Matteo’s request; the homepage keeps a broad introduction and focuses on completed work.
- The shorter undergraduate summary omits the older percentage/error figures: the MATS profile review records that their denominator and comparator need a clearer source. The methods description remains supported by the CV.
- Research diagrams: rendered without alteration from Matteo's Sparse Readout Prism arXiv source bundle (`SRP/arxiv_upload/source/figures/schematic_tikz/readout_query_one_column.pdf`, `prism_icon.pdf`, and `proto_token_lens/qwen2b_32x_domain_features_main3/qwen2b_32x_polysemy_delta_row_onecol.pdf`). These are real paper figures, not simulated research results.
- Learning to Read Out result figure: copied without alteration from `Desktop/LRO/Learning_to_Read_Out/figures/readout_coordination/sva_availability_expression_69b.png`. The caption and scope were checked against `main.tex`, figure `fig:main-sva-availability-expression`. It shows subject–verb agreement during training and across difficulty levels at convergence in Pythia-6.9B. Included at the user's request; the manuscript PDF is not bundled.
- Interactive feature trajectories: `public/research/trajectories.json` is exported by `scripts/export-trajectories.py` from the four `*_decoder_norms.npy` caches in `Desktop/LRO/unembedding_development/figures/feature_lifecycle_trajectories/section52_lifecycle/cache`. Active threshold and normalization match the paper plot script (peak norm > 0.01; divide by each feature's own maximum). Full-population medians at all checkpoints match the released `selected_decoder_norm_trajectories.csv` exactly after the paper's float16 conversion. Each model displays 1,200 real curves selected at evenly spaced ranks after stable sorting by peak checkpoint, with values rounded to four decimal places; this is disclosed below the chart. Colours use a common log peak-step scale from 0 to 928,000. Replay reveals measured checkpoints; lines join observations without fitted smoothing. Feature numbers are original dictionary indices. The static fallback is rendered from the paper's `selected_decoder_norm_trajectories.pdf`, with the full populations. The agreement figure now sits under Method & scope.
- Font: locally hosted DM Sans, reused from the career-ops font assets. License in `public/fonts/OFL.txt`, sourced from https://github.com/google/fonts/blob/main/ofl/dmsans/OFL.txt. Georgia uses the visitor's system font installation.

The page describes completed degrees rather than implying current Cambridge employment. The language-learning platform is explicitly archived. No private unpublished manuscript PDF is included. No analytics or third-party runtime requests are added to the profile.

Latest contact preference: Matteo explicitly supplied `matteohe.research@gmail.com` for public use. The homepage and public CV display this address with direct mail links. The form, provider configuration, and reveal controls have been removed. Canonical application CVs are unchanged.

Public CV revision: at Matteo's request, the website PDF now uses the academic layout and research content of `output/cvs/drafts/mhcv.pdf`, with matching source recovered from `matteo-he-mats-winter-2027.tex`. The public email is updated, all five Learning to Read Out bullets are retained, coursework is removed, Amazon has a dated industry entry grounded in `cv.md`, and prose is simplified while retaining scientific scope and numerical results. GRE 340/340 and the Top Student Medal remain. The revised PDF is one page with verified email and research links; the original mhcv and canonical application PDFs are unchanged.
